import { css } from '@emotion/react'

const fill = css`
  flex-grow: 1;
`

const flexCol = css`
  display: flex;
  flex-direction: column;
`

const styles = {
  fill: fill.name,
  flexCol: flexCol.name,
}

export default styles
